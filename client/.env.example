# Base URL for API calls. In development this is proxied to the Flask
# server on port 5000 (see vite.config.js). In production, set this to
# your deployed API origin, e.g. https://api.example.com/api
VITE_API_BASE_URL=/api
